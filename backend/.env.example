# Tencent TokenHub (China mainland)
TOKENHUB_API_KEY=replace_me

# OpenAI-compatible Base URL.
AI_BASE_URL=https://tokenhub.tencentmaas.com/v1

# Recommended first-pass models
AI_TEXT_MODEL=hy3
AI_VISION_MODEL=hy-vision-2.0-instruct

# Frontend origin during development can be *
# For the deployed site, use the exact origin (or comma-separated origins):
#   https://fittool-lias-fittool-cn-d4f4epz09632b25c.webapps.tcloudbase.com
# This value lives in the CloudBase console environment variables, never in the repo.
ALLOWED_ORIGIN=*

# Fail fast instead of waiting almost one minute.
AI_TIMEOUT_MS=22000
